// src/app/create-fund/layout.tsx
import React from 'react';

export default function CreateFundLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      {children}
    </div>
  );
}